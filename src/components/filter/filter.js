import React from "react";
import './filter.scss'

const Filter = props => {
    return (
        <div className='filter'>
            <select>

            </select>
        </div>
    )
};

export default Filter;
